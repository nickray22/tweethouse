<h1>Hello, Tweethouse Users!</h1>

<ul>
  <% @users.each do |user| %>
    <li>Hello, <%= user.username %>.
      Actions:
      <%= link_to "Your Shouts", user_shouts_path(user) %> |
      <%= link_to "Delete Account", user_path(user), method: :delete %> |
      <%= link_to "Create Shout", new_user_shout_path(user) %>
    </li>
  <% end %>
</ul>

<%= link_to 'Back', :back %> | <%= link_to 'New User', new_user_registration_path %>
